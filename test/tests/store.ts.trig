PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>

GRAPH <createStore/beforeEach,-trig/graph-exists> {
  [
    rdfs:label "Trig used in beforeEach" ;
  ] .
}

GRAPH <another-graph> {
  [
    rdfs:label "Label from another graph" ;
  ] .
}
